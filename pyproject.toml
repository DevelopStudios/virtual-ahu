[project]
name = "virtual-ahu"
version = "0.1.0"
description = "FastAPI‑based virtual AHU simulator"
requires-python = ">=3.10"

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.115.0"
uvicorn = "^0.30.0"
pydantic = "^2.8"

[tool.poetry.dev-dependencies]
pytest = "^8.3"